---
interface Props {
    currentPage?: string;
}

const { currentPage = 'home' } = Astro.props;

// Use hash links on homepage, full path links on other pages
const linkPrefix = currentPage === 'home' ? '' : '/';
---

<!-- Skip Link for Accessibility -->
<a href="#main-content" class="skip-link">Skip to main content</a>

<!-- Navigation -->
<nav class="nav" role="navigation" aria-label="Main navigation">
    <div class="nav-container">
        <a href="/" class="logo" aria-label="fullai.dev Home">
            <div class="logo-group">
                <img src="/logo.png" alt="fullai.dev" class="logo-img">
                <span class="logo-text">fullai<span class="dot">.</span>dev</span>
            </div>
        </a>
        <div class="nav-links">
            <a href={`${linkPrefix}#services`} data-en="Services" data-es="Servicios">Services</a>
            <a href={`${linkPrefix}#approach`} data-en="Approach" data-es="Enfoque">Approach</a>
            <a href="/blog" class:list={[{ active: currentPage === 'blog' }]} data-en="Blog" data-es="Blog">Blog</a>
            <a href={`${linkPrefix}#contact`} class="nav-cta" data-en="Let's Talk" data-es="Hablemos">Let's Talk</a>
            <button class="lang-switch" id="langSwitch" aria-label="Switch language">
                <span class="flag" id="currentFlag">🇺🇸</span>
                <span class="lang-code" id="currentLang">EN</span>
            </button>
        </div>
    </div>
</nav>

<style>
    /* Skip Link for Accessibility */
    .skip-link {
        position: absolute;
        top: -100px;
        left: 50%;
        transform: translateX(-50%);
        background: var(--color-primary);
        color: var(--color-bg);
        padding: 0.75rem 1.5rem;
        border-radius: 8px;
        font-weight: 600;
        text-decoration: none;
        z-index: 9999;
        transition: top 0.3s var(--ease-smooth);
    }

    .skip-link:focus {
        top: 1rem;
        outline: 3px solid var(--color-accent);
        outline-offset: 2px;
    }

    /* Navigation */
    .nav {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 1000;
        background: rgba(10, 14, 23, 0.8);
        backdrop-filter: blur(12px);
        border-bottom: 1px solid var(--color-border);
        animation: slideDown 0.6s var(--ease-smooth);
    }

    @keyframes slideDown {
        from {
            transform: translateY(-100%);
            opacity: 0;
        }
        to {
            transform: translateY(0);
            opacity: 1;
        }
    }

    .nav-container {
        max-width: 1280px;
        margin: 0 auto;
        padding: 1.25rem 2rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .logo {
        text-decoration: none;
    }

    .logo-group {
        display: flex;
        align-items: center;
        gap: 0.75rem;
    }

    .logo-img {
        width: 40px;
        height: 40px;
        border-radius: 50%;
        animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-5px); }
    }

    .logo-text {
        font-family: var(--font-display);
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--color-text);
    }

    .dot {
        color: var(--color-primary);
    }

    .nav-links {
        display: flex;
        gap: 2rem;
        align-items: center;
    }

    .nav-links a {
        color: var(--color-text-dim);
        text-decoration: none;
        font-size: 0.9rem;
        font-weight: 400;
        transition: all 0.4s var(--ease-smooth);
        position: relative;
        padding: 0.5rem 0;
    }

    .nav-links a:hover,
    .nav-links a.active {
        color: var(--color-primary);
        text-shadow: 0 0 15px rgba(0, 255, 163, 0.4);
        transform: translateY(-2px);
    }

    .nav-links a::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 50%;
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, var(--color-primary), var(--color-accent));
        transition: all 0.4s var(--ease-smooth);
        transform: translateX(-50%);
        border-radius: 2px;
        box-shadow: 0 0 10px rgba(0, 255, 163, 0);
    }

    .nav-links a:hover::after,
    .nav-links a.active::after {
        width: 100%;
        box-shadow: 0 0 10px rgba(0, 255, 163, 0.5);
    }

    .nav-links a.nav-cta {
        background: var(--color-primary);
        color: var(--color-bg) !important;
        padding: 0.5rem 1.75rem !important;
        border-radius: 6px;
        font-weight: 500;
        box-shadow: 0 0 20px rgba(0, 255, 163, 0);
        transition: all 0.4s var(--ease-smooth);
    }

    .nav-cta::after {
        display: none;
    }

    .nav-cta:hover {
        background: var(--color-primary);
        transform: translateY(-3px) !important;
        box-shadow: 0 5px 25px rgba(0, 255, 163, 0.4);
    }

    .nav-cta:active {
        transform: translateY(-1px) !important;
        box-shadow: 0 2px 15px rgba(0, 255, 163, 0.3);
    }

    /* Language Switch */
    .lang-switch {
        position: relative;
        display: flex;
        align-items: center;
        gap: 0.6rem;
        background: var(--color-surface-elevated);
        border: none;
        border-radius: 50px;
        padding: 0.5rem 1rem 0.5rem 0.6rem;
        cursor: pointer;
        transition: all 0.4s var(--ease-smooth);
        font-family: var(--font-mono);
        font-size: 0.8rem;
        color: var(--color-text);
        overflow: hidden;
    }

    .lang-switch::before {
        content: '';
        position: absolute;
        inset: 0;
        background: linear-gradient(135deg, var(--color-primary), var(--color-accent));
        background-size: 200% 200%;
        opacity: 0;
        transition: opacity 0.4s var(--ease-smooth);
        animation: gradientMove 3s ease infinite;
    }

    @keyframes gradientMove {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    .lang-switch:hover::before {
        opacity: 0.85;
    }

    .lang-switch:hover {
        transform: scale(1.05);
        box-shadow: 0 6px 25px rgba(0, 255, 163, 0.5);
    }

    .lang-switch:active {
        transform: scale(0.98);
    }

    .lang-switch .flag {
        position: relative;
        z-index: 1;
        font-size: 1.3rem;
        line-height: 1;
        filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
        transition: transform 0.3s var(--ease-bounce);
    }

    .lang-switch:hover .flag {
        transform: scale(1.15) rotate(-5deg);
    }

    .lang-switch .lang-code {
        position: relative;
        z-index: 1;
        font-weight: 600;
        font-size: 0.75rem;
        letter-spacing: 0.05em;
        color: var(--color-text-dim);
        transition: all 0.3s var(--ease-smooth);
    }

    .lang-switch:hover .lang-code {
        color: var(--color-bg);
    }

    /* Mobile */
    @media (max-width: 968px) {
        .nav-links a {
            display: none;
        }

        .nav-links a.nav-cta {
            display: none;
        }

        .nav-container {
            padding: 1rem;
        }

        .logo-img {
            width: 32px;
            height: 32px;
        }

        .logo-text {
            font-size: 1.25rem;
        }
    }
</style>
